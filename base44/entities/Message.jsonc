{
  "name": "Message",
  "type": "object",
  "properties": {
    "conversation_id": {
      "type": "string"
    },
    "sender_email": {
      "type": "string"
    },
    "sender_name": {
      "type": "string"
    },
    "sender_role": {
      "type": "string",
      "enum": [
        "buyer",
        "shop"
      ]
    },
    "content": {
      "type": "string"
    },
    "read": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "conversation_id",
    "sender_email",
    "content"
  ],
  "rls": {
    "select": {
      "formula": "record.sender_email == user.email || user.role == 'admin' || user.role == 'shop_owner'"
    },
    "insert": {
      "formula": "request.sender_email == user.email"
    },
    "update": {
      "formula": "record.sender_email == user.email || user.role == 'admin'"
    },
    "delete": {
      "formula": "user.role == 'admin'"
    },
    "create": {
      "created_by": "{{user.email}}"
    },
    "write": {
      "formula": "record.sender_email == user.email || user.role == 'admin'"
    }
  }
}